name: notification_cleaner
description: Programmatically clear all notifications from the system tray with this lightweight Flutter plugin. Supports modern Android and iOS implementations with a simple, minimal API.
version: 0.0.5
homepage: https://github.com/mr-gami001/notification_cleaner
repository: https://github.com/mr-gami001/notification_cleaner
issue_tracker: https://github.com/mr-gami001/notification_cleaner

topics:
  - notifications
  - flutter-plugin
  - android

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.example.notification_cleaner
        pluginClass: NotificationCleanerPlugin
      ios:
        pluginClass: NotificationCleanerPlugin
        spm:
          packageName: notification_cleaner

